language: node_js
node_js:
  - "node"
install:
  - npm i -g npm@5.8.0
  - npm ci
script:
  - npm run test
branches:
  only:
    - master
    - dev
before_script:
  - npm run lint
cache:
  directories:
    - "$HOME/.npm"
