language: node_js
node_js:
  - "node"
install:
  - npm install
script:
  - npm run test
branches:
  only:
    - master
    - dev
before_script:
  - npm run lint
cache:
  directories:
    - "$HOME/.npm"
