const assert = require('assert')
const wavematch = require('../src/index')

// ## Array
// - Non-greedy:
//   - empty array
//   - array of length 1
//   - array of length N
// - Greedy:
//   - Match an array of arbitrary length
//   - Match an array of at least length 1
//   - Match an array of at least length N

// const val = 'hi' // doesn't matter what actual value is
// const nonArrayDataTypes = [ 0, 4.2, '', {}, () => {}, void 0, null, /.*/ ]

describe('array', () => {
})
